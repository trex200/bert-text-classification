# Data

Notebook file: **`sst2_train.csv`**

- 67349 rows
- columns `sentence`, `label` (0 = neg, 1 = pos)
- SST-2 / GLUE binary sentiment

Drop at `data/sst2_train.csv` when training. Not committed.
